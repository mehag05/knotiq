{
    "name": "knot-data-demo",
    "version": "1.0.0",
    "description": "Knot Data Demo Application",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "build": "cd client && npm install && npm run build",
        "dev": "concurrently \"cd client && npm start\" \"nodemon server.js\"",
        "install-all": "npm install && cd client && npm install"
    },
    "dependencies": {
        "express": "^4.18.2",
        "sqlite3": "^5.1.6",
        "dotenv": "^16.0.3"
    },
    "devDependencies": {
        "concurrently": "^8.0.1",
        "nodemon": "^2.0.22"
    }
}
